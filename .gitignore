# Zsh
zsh/.zcompdump*
zsh/.zsh_history
zsh/.zsh_sessions

# Command folders and files
htop

# Claude Code
.claude

# Crossnote
crossnote

# Expert LSP
.expert

# GitHub Copilot
github-copilot

# GPG
gnupg

# Homebrew
homebrew/trust.json

# SwiftPM
swiftpm

# UV
uv/uv-receipt.json

# Android
.android

# Agent of Empires (aoe) — track config.toml, ignore generated runtime state
agent-of-empires/*
!agent-of-empires/config.toml

# Claude Code — skills synced from claude.ai (sync disabled via syncClaudeAiSkills)
claude/skills/synced
claude/skills/.trash
